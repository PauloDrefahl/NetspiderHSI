# input the clean xlsx file

# preprocess into text where the model can read and predict keywords

# define the model
 # bert.predict(dataset, given keywords)
# run the model

# extract results / save